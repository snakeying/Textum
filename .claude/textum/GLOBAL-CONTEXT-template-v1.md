# [项目名称] - 全局上下文

> ⚠️ 每个Story执行前必读本文件
> 规则编号: `BR-###`（三位数字，001 起递增且唯一）；引用规则: 在 Story 中用 `GC#BR-###` 引用第 4 节业务规则；其余全局约定默认生效，不在每个 Story 重复描述
> 本文件由 `docs/PRD.md` 抽取生成：不得新增 PRD 中不存在的新规则/新枚举/新接口/新字段；如发现缺口就停止并让用户确认
> 占位符约定: `N/A`=不适用且无需回填；`TBD`=待回填（仅允许出现在第 4 节“涉及Story”和第 9 节依赖图，完成 `/backfill` 后应消除）

## 1. 技术栈（必填）

| 层 | 选型 |
|----|------|
| 后端 | [框架/语言] |
| 前端 | [框架/库] |
| 数据库 | [类型] |
| 其他 | [缓存/消息队列/...] |

## 2. 项目结构（必填）

```
project/
├── [目录]/
│   └── [子目录或文件]
└── ...
```

## 3. 枚举值定义（必填；无则写 N/A）

| 字段 | 可选值 | 说明 |
|------|--------|------|
| [表.字段] | [val1, val2, ...] | [用途] |

## 4. 业务规则（必填）

| ID | 规则 | 涉及Story |
|----|------|-----------|
| BR-### | [规则描述] | TBD |
| BR-### | [规则描述] | TBD |

## 5. 权限矩阵（必填）

> 记号建议: `A=允许` `D=禁止` `O=仅自己的/所属组织(资源范围)`

| 操作 | [角色1] | [角色2] | [角色N] |
|------|---------|---------|---------|
| [操作] | A/D/O | A/D/O | A/D/O |

## 6. 数据模型概览（必填；如无 DB 写 N/A）

### 表关系
```
[表A] ─< [表B] >─ [表C]
```

### 全局字段约定（如适用；否则写 N/A）
| 字段 | 说明 |
|------|------|
| `id` | 主键 |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |
| `[tenant_id/org_id]` | 多租户/组织隔离（如适用） |
| `[deleted_at/is_deleted]` | 软删除（如适用） |

### 核心表
| 表 | 主要字段 |
|----|----------|
| [表名] | [字段列表] |

## 7. 命名规范（如适用；否则写 N/A）

| 类别 | 规范 | 示例 |
|------|------|------|
| 表名 | [规范] | [示例] |
| 字段 | [规范] | [示例] |
| API | [规范] | [示例] |
| 组件 | [规范] | [示例] |

## 8. API规范（必填）

### 基础（必填）
- Base URL: `[例如: /api]`
- 认证方式: [JWT/Cookie/Session/...]
- 权限校验: [按第 5 节权限矩阵执行]
- 分页/排序/筛选: [是否支持 + 参数约定（例如 `page/pageSize` 或 `cursor`，`sort/order`）]

### 响应格式
```json
{ "data": {}, "message": "" }
```

### 错误码（必填）
| 状态 | 说明 |
|------|------|
| [4XX] | [场景] |

## 9. Story依赖图（必填；拆分后回填）

TBD
